import lists.ListContainer;
import lists.ListTestExecutor;
import maps.MapContainer;
import maps.MapTestExecutor;

public class App {
    public static void main(String[] args) {
        //ListTestExecutor.check(new ListContainer());
        MapTestExecutor.check(new MapContainer());
    }
}